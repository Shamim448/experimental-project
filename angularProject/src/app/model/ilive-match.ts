export interface IliveMatch {
    matchHeading:string;
    matchNo:string;
    matchLocation:string
    batingTeam:string
    score:string
    bowlingTeam:string
    bScore:string
    status:string
}
